version: 2

models:
  - name: gift_card_orders
    description: "Orders that include a gift card amount greater than 0"
    columns:
      - name: order_id
        description: "The unique identifier of the order"
        tests:
          - unique
          - not_null